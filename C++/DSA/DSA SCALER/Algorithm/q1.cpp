#include<iostream>
using namespace std;
int devide(int a,int arr[]){
    int sum=0,y;
    int left=0,right=a-1;
    int mid =(left+right)/2;
    if(int mid !=decimal){
        return 1;
    }
    else {
        return 0;
    }
}
int main()
{
    int a;
    scanf("%d",&a);
    int arr[a];
    for(int i=0;i<a;i++)
    {
        scanf("%d",&arr[i]);
    }
    devide(arr.size(),arr[a])
    while(devide()!=1){
    right++;
    int mid =mid +1;
    }
 return 0;
}