#include <iostream>
#include <cmath>
using namespace std;

int main()
{
    cout << sqrt(64) << "\n";   // Gives the  square root
    cout << round(2.6) << "\n"; // Roundoff the number
    cout << log(2) << "\n";     // Gives the log value
    return 0;
}
